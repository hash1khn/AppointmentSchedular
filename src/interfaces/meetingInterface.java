package interfaces;

/** Interface for use with lambda expression. Displays meeting reminder. */
public interface meetingInterface {

    /** Method which is used on the login page for use with lambda expression. Displays meeting reminder. */
    //lambda expression used with this interface to remind the user for approaching appointments.
    String getReminder();
}
